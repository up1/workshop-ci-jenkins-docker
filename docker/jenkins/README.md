# Jenkins with Docker

## Build image
```
docker compose build
```

## Create container
```
docker compose up -d
docker compose ps
docker compose logs --follow
```

Access to jenkins server = http://localhost:8080